1. Make it work
    Visualize the porblem as a tree
    Implement the tree using recursion
    Test it

2. Add a memo object
    Add a base case to return memo values
    Stire return values into the memo